---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: podinfo-tls-certificate
  namespace: podinfo
spec:
  secretName: podinfo-tls-secret
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: podinfo.halvorteigen.no
  dnsNames:
    - podinfo.halvorteigen.no